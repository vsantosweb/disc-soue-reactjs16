

export const setDiscStep = (step) => ({
    type: 'SET_CURRENT_STEP',
    payload: step
});

export const setDiscSession = (sessionData) =>({
    type:'SET_DISC_SESSION',
    payload: sessionData
});
